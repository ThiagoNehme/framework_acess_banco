export interface Livro {
    _id?: string; // Compatível com MongoDB
    titulo: string;
    autor: string;
    ano: number;
}
export interface Livro {
    _id?: string; // Compatível com MongoDB
    titulo: string;
    autor: string;
    ano: number;
}
